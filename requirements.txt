# ============================================================
# MAXY — Sistem Rekrutmen Lintas Maritim
# Dependensi Python (uji pada Python 3.12)
# Install:  pip install -r requirements.txt
# ============================================================

# --- Web server ---
Flask==3.1.3
gunicorn==23.0.0            # untuk produksi (opsional saat dev)

# --- Konfigurasi ---
python-dotenv==1.0.1

# --- AI (Claude): CV match, analisis video, CAPTCHA vision ---
anthropic>=0.40

# --- HTTP & verifikasi sertifikat (RPA) ---
requests==2.33.0
playwright==1.56.0          # jalankan: playwright install chromium

# --- Ekstraksi CV (PDF) ---
pypdf>=4.0

# --- Generate kontrak PKWT ---
reportlab==4.4.10           # PDF
python-docx==1.2.0          # DOCX

# --- Gambar (CAPTCHA, dsb) ---
pillow>=10.0

# ============================================================
# OPSIONAL — transkripsi video (pilih salah satu provider STT)
# ------------------------------------------------------------
# STT_PROVIDER=openai  -> pakai requests (sudah ada) ke OpenAI Whisper API
# STT_PROVIDER=groq    -> pakai requests (sudah ada) ke Groq Whisper API
# STT_PROVIDER=local   -> transkripsi lokal (tanpa biaya API), perlu:
# faster-whisper>=1.0
# ============================================================
# Catatan: ffmpeg WAJIB tersedia di sistem (bukan paket pip).
#   Ubuntu/Debian:  sudo apt-get install -y ffmpeg
